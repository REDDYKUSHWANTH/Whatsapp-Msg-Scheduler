services:
  # Web service - WhatsApp Scheduler
  - type: web
    name: whatsapp-scheduler
    env: docker
    plan: standard
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: SESSION_SECRET
        generateValue: true
      - key: MONGODB_URI
        fromService:
          type: database
          name: mongodb
          property: connectionString
      # Uncomment and configure these as needed
      # - key: SMTP_HOST
      #   value: smtp.example.com
      # - key: SMTP_PORT
      #   value: 587
      # - key: SMTP_SECURE
      #   value: false
      # - key: SMTP_USER
      #   value: user@example.com
      # - key: SMTP_PASS
      #   sync: false # This will prompt for the value during deployment
      # - key: EMAIL_FROM
      #   value: no-reply@example.com
      # - key: GOOGLE_CLIENT_ID
      #   sync: false
      # - key: GOOGLE_CLIENT_SECRET
      #   sync: false
      # - key: GOOGLE_CALLBACK_URL
      #   value: https://your-app-name.onrender.com/auth/google/callback
    disk:
      name: whatsapp-data
      mountPath: /usr/src/app/uploads
      sizeGB: 10
    autoDeploy: true
    healthCheckPath: /
    numInstances: 1

  # MongoDB database
  - type: database
    name: mongodb
    plan: free # Upgrade to a paid plan for production
    databaseName: whatsapp-scheduler
    ipAllowList:
      - source: 0.0.0.0/0
        description: everywhere

